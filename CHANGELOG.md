# Changelog

## 1.5.0

- 专业模式新增单一配对方案、处理—对照一一映射、匹配标签及组内原始顺序/显式 ID 配对；映射不完整时整次分析停止，未映射和未分配拆分水平按审核规则排除。
- 新增最多 10 个配对计算列，每列限定同一原始指标的处理值、对照值、基础算术、括号、自定义名称和最多 8 位小数；配对列只能作为因变量。
- 普通自定义列可在配对后引用处理侧原始列或配对列，仍禁止普通自定义列互引，形成无环的单向依赖链。
- 联合因变量组可使 7d、14d、21d 等多组 MANOVA 在一个计划中与拆分和因素组合共同展开。
- MANOVA 新增“按交互分支完整比较”策略，覆盖参考 R 脚本的边际均值与条件简单效应分支，同时保留 v1.0 的保守默认策略。
- 执行审核分层展示输入范围、配对质量、计算列、模型结构和最终任务树；旧后端缺少 V1.5 能力标志时前端阻止执行。
- R 4.6.1/Python 双验证通过全部 47 种方法、133 个字段及农业参考链路；前端生产构建与 V1.5 回归通过。R 依赖仅用于开发校验。
- 生成 V1.5 多基线服务器热补丁，可识别原始 0.4.9、旧自定义列热补丁和 1.0.0 网站部署版；安装前校验哈希并完整备份，支持回滚，不包含虚拟环境或用户数据。

完整设计与实施验收见 [`docs/V1.5_UPDATE_PLAN.md`](docs/V1.5_UPDATE_PLAN.md)。

## 1.0.0

- 首个正式稳定版本，统一 Python、Vue、Tauri、服务器健康接口和发布清单版本号。
- 专业模式新增最多 10 个自定义计算列：每列最多引用 10 个原始数值列，支持基础算术与括号，结果最多保留 8 位小数，并禁止自定义列循环或链式引用。
- 自定义列仅作为因变量使用，自动继承来源列和全局拆分的排列组合；不允许作为分类因素、协变量或独立拆分列。
- 补齐全部 47 种统计方法与 Python 计算依赖的项目组对照文档，并以文档契约测试防止注册表与手册漂移。
- 使用 R 4.6.1 与 Python 计算核心对全部 47 种可运行方法执行开发期双重校对；农业参考另覆盖 CK/T 配对、NDR/Delta、Type III ANOVA 和 Wilks MANOVA。R 不进入正式运行或服务器发布依赖，入口见 [`docs/REPRODUCIBILITY.md`](docs/REPRODUCIBILITY.md)。
- main 分支保留完整可开发源码，Web 分支保留服务器部署源码及 0.4.9 升级热补丁；清理缓存、测试临时目录、依赖目录和旧发布产物。

## 0.4.9

- 小助手改为以用户选择为准的自由 Markdown 问答：默认只显示简要的六项工作流信息，数据格式在导入后默认勾选，其他上下文由用户手动选择；完成选择后程序不再擅自改写用户的上下文开关。
- 网站部署新增工作区所有者认证、最多 3 个活跃用户、2 分钟心跳、10 分钟无实际操作退出，以及服务器共享 AI 配置。匿名访客每个浏览器会话可使用 10 次服务器 AI，首次提问 24 小时后恢复；工作区认证或自带 API 不受此限制。
- 服务器共享 AI 密钥仅保存于服务器本机受限权限文件，不写入网页、源码通用包或热补丁；匿名执行分析不会在后台自动消耗 AI 额度。
- Windows x64 重新使用 Visual Studio Build Tools 与 Rust/Tauri 原生工具链构建中文 NSIS 安装器和便携 ZIP；sidecar 独立冒烟覆盖健康检查、47 个方法、真实分析和报告 ZIP。
- AI 小助手的“当前分析结果”上下文新增结果时间戳，按本地时区显示到秒；即时分析使用结果完成时间，项目运行优先读取持久化完成时间并回退到开始时间，结果失效时同步清除旧时间。
- 重构 AI 小助手悬浮窗的信息层级：采用紧凑标题栏、带说明与状态的“回答依据”选择行、可折叠工作流信息、独立消息滚动区和固定输入区；桌面默认尺寸调整为 440 × 760，移动端继续使用安全边距近全屏布局。当前结果新增“组别排序（A > B > C）”独立上下文开关，关闭后前后端均不再发送或注入排序结论。
- 完成发布前资产与文档治理：新增可审查的缓存清理脚本，源码归档排除审计/临时/截图/旧 sidecar，移除未使用导入和完全重复审计副本；新增项目规范总册、测试报告总档和发布清单，开发日志、用户手册、测试源码与历史 Changelog 全部保留。
- AI 小助手新增可审计上下文选择：当前分析结果默认只发送本地排序、三张规范表和前提检验，可切换为完整结果与诊断；当前运行的 AI 文字报告可作为低优先级解释材料继续提问。所有回答回显实际依据，未选择内容不进入模型请求，新运行会自动使旧报告上下文失效。
- 组别序列化排序改为单一限高滚动区：不再把前10项与其余项目拆成两段，全部排序保持同一优先级序列并由整个列表统一上下滚动。
- AI 报告提示词改为证据链综合：依次连接研究设计、总体检验与校正、条件性效应、只读排序、效应量、前提检验和解释边界，主动提炼稳定规律、条件性规律与例外，规范表负责完整数值陈列。
- 优化 AI 报告状态与正文呈现：AI 已生成时，本地证据页明确引导切换到第二页，不再显示“未调用外部 AI”的矛盾提示；AI 正文改为每节三条结论优先、其余统计细节折叠，并自动隐藏 `read_only_ordering` 等内部字段名。
- 收紧批量 AI 写作规范：禁止逐任务串列 p 值和成对比较，推断段落只概括显著模式、校正口径与解释边界，完整统计值继续引用三张规范表和本地只读排序。
- 修复拆分批量规范表的效应量错位：批量总览始终保留任务 ID，旧结果也会按拆分条件恢复任务身份；相同任务、任务条件与效应的 η²、偏 η²、ω²等指标合并到表2原检验行的同一效应量列，不再拆成多条补充行。
- AI 规范报告输出契约改为只生成文字字段，不再要求模型回传本地排序和三张大表；兼容带简短前言的完整 JSON 响应，并在连续空响应或结构不完整时显示明确原因，降低批量报告因输出过长而无法应用的概率。
- 规范报告效应量改与推断检验合并至表2：Cohen's d、Hedges' g、η²、偏 η²、ω²及可用置信区间优先附在所属检验行，无法可靠对应的项目才作为表2补充效应量行保留；AI 证据包同步加入结构化前提检验统计量、p 值、通过判断与球形性校正参数。
- AI 文字总结页不再重复本地排序、证据概览和规范表，只显示通过守卫的优化文字；本地排序按“显著性字母层级 → 估计边际均值 → 描述性均值”排序，并在单一限高列表中整体滚动查看。
- 顶部五步工作流改为可点击导航：已有步骤直接跳转并短暂高亮，目标尚未生成时回退到当前可操作的前置步骤；规范报告宽表新增独立横向滚动容器，窄窗口和多列结果不再被裁切。
- 规范报告改为“本地证据先显示、AI 文字后台生成”：本地不再生成文字正文，只立即展示可折叠的 `A > B > C` 只读排序与表1—表3；AI 成功后自动切换到文字总结页，批量总结默认收起，未应用时保留本地证据并提供重试入口。
- 修复逐批校正表误读完整结果长表的问题：每个批次现在与总览使用相同的主要/总体检验口径，只显示具有有效原始 p 的推断行；因素水平、EMM、两两比较和字母分组保留在各自结果区，不再显示“未提供原始判断”或被误纳入跨任务校正。
- 新增跨模型校正设置一致性拦截：预检与执行结果都必须与页面所选的“不校正/Holm/Bonferroni/FDR-BH”一致；检测到未重启的旧后端返回不同设置时直接阻止分析，不再静默显示错误的“校正后显著”。
- AI 文字总结只接收三张规范表、只读排序、统计方法、数据角色与相关列画像；隐私设置允许时最多附 20 行参与分析列的数据预览。排序禁止重排，数据预览禁止用于重新计算或覆盖规范表结论。
- 修复 AI 规范报告数值守卫误审本地统计表号和数值型因素标签的问题：守卫仅检查 AI 可改写叙述；不合格章节直接移除，不再用内置文字回填，连续失败时展示具体原因并保留本地排序与三表。
- 本地证据页、AI 文字总结与结果解读共用确定性 `A > B > C` 式组别排序；排序优先使用 CLD/EMM 并明确区分数值顺序与显著差异。报告页新增自由提问入口，自动同步当前完整结果与问题到 AI 助手。
- 修复跨模型选择“不校正”后的显示与导出语义：仅报告原始 p 与“原始显著/不显著”，不再生成数值相同的“校正 p”或“校正后显著”；启用校正时同时展示校正前、校正后判断。专业模式支持编辑因素组合显示名，并保留原始组合因素用于追溯。
- 规范统计报告按参照格式固定生成三张核心表：设计单元描述统计（M ± SD）、主要总体/多变量检验、各因变量单变量及后续检验；MANOVA 主体间效应表新增 SS 与 MS，并在正文中明确引用表1—表3。
- 完成跨模型校正审计：新增 `cross_model_p_adjust` 规范字段并兼容旧 `combination_p_adjust`；简洁/专业模式均可设置，同批多因变量、因素组合和数据拆分统一进入校正，预检新增多模型与重复运行双重提醒。
- 新增四至八因素 ANOVA 与 MANOVA 完整析因模型；即时分析明确禁用高阶因素组合。简洁模式保留原有按值拆分，专业模式新增类别水平合并与数值区间分组，预检、执行和导出共用同一拆分规则。
- 清理重复浏览器调试脚本、旧构建引用、静默异常和过期字段说明，并补充运行时数据库、pytest/UI 临时产物及 Python 编译缓存的忽略规则；开发文档、版本记录、用户手册与测试代码完整保留。
- 新增独立 AI 规范统计文字总结，可单独下载 Markdown，并用结构化 Schema 与 canonical JSON 本地数值守卫阻止 AI 补造统计量。
- 规范报告扩展至多次、因素组合和数据拆分模型：计算完成即先显示零外部请求的本地排序与三表，AI 完成后增加第二页翻页切换；合并报告统一提供 XLSX、完整 ZIP、Markdown 与 canonical JSON 四类导出。
- 修复 Stuart–Maxwell/Bhapkar 奇异协方差输入返回 NaN；批量拆分子集现在逐项重新预检，Fleiss/Randolph kappa 不再把退化子集带入结果。
- 完成 Windows x64 单文件 sidecar 与便携 ZIP 构建；新增独立进程分析/报告冒烟、sidecar 复用构建、便携包脚本和桌面 npm 锁文件。
- 源码打包明确排除 Tauri 平台 sidecar、桌面 `node_modules` 与 Rust `target`，避免源码 ZIP 混入大体积生成产物。
- 新增冻结黄金数据集：29 个 CSV、52 个案例、425 个独立断言；覆盖全部 47 个当前可执行方法，并保留旧析因计划迁移案例。
- 新增 `scripts/run_golden_validation.py`、黄金数据再生脚本和发布回归测试，输入数据使用 SHA-256 锁定。
- t 检验改为主要结果直接报告带方向的 t、自由度、p、均值差区间和效应量，不再以 `t²` 包装为总体 F 结果。
- Fleiss κ 不再用 `p=1` 伪装推断结论；无推断 p 值时网页和报告明确显示“未提供推断检验”。
- ANOVA 与事后比较核心不再提前舍入 p 值；极小 p 保留完整浮点精度，显示层再格式化。
- 经典混合 ANOVA 成对比较生成真实多重校正置信区间，不再输出 `[0,0]` 占位。
- 工作区跨运行比较同时读取主要检验和总体检验，t 检验运行比较不再为空。
- MANOVA 普通默认改为 Pillai；全部四种多元判据保留在专业模式。
- 简洁模式每个方法最多显示 1–2 个常用参数；常用选项新增推荐理由和一句话解释，高级参数默认隐藏但完整保留。
- 修复 AI 悬浮窗八方向缩放的 CSS 覆盖顺序和裁剪问题，真实浏览器拖动/缩放审查通过。
- 删除重复的统一 `factorial_anova` 阶数控制入口，新建分析直接选择单因素、双因素或三因素 ANOVA；旧计划在读取时自动迁移。
- 批次结果页改为简洁总览与逐批结果切换，分析参数移到结果之后；批次 Excel 改为总览、逐批结果、失败与警告、分析设置多工作表结构。
- 260 项自动化测试、344 组高级参数变体、387 组数据结构变体（190 个合法配置全部执行成功且无非有限结果）通过。

## 0.4.8

- 即时单次分析新增统一报告生成接口，t 检验、非参数检验、相关、回归等均可下载结果 Excel、Markdown 与完整报告 ZIP。
- 参数界面默认改为“简洁模式”：使用推荐默认值，仅按需展开常用设置；显著性水平、平方和、诊断图、EMM 和低频参数移入“专业模式”。
- 结果页默认显示核心检验、p 值、效应量和结论；自由度、置信区间、诊断、描述统计、EMM 与完整成对比较改为按需展开。
- 新增统一 `AnalysisResultView`，即时分析和项目工作区使用同一结果呈现逻辑，避免两套表格字段不一致。
- AI 助手悬浮窗升级为八方向缩放，支持左右停靠、最大化/还原、最小化、默认布局恢复、可视视口边界约束与持久化。
- 常规界面默认关闭诊断图生成，减少无需求时的计算和报告复杂度；专业模式可随时启用。
- 166 项自动化测试通过，前端 TypeScript 与生产构建通过，即时报告 ZIP/XLSX 完成真实下载验证。

## 0.4.7

- 将统一 MANOVA 拆分为 `oneway_manova`、`twoway_manova`、`threeway_manova` 三个固定入口，分别严格要求 1、2、3 个对象间分类因素。
- 删除新 MANOVA 分析中的 `factor_model_order` 参数，消除方法范围与阶数参数的双重控制。
- 修复即时分析和项目工作区在因素已选完整后仍显示“缺少必要设置”的问题；缺失提示改为“恰好需要 N 个分类因素”。
- 固定阶数方法的组合面板不再显示最小/最大阶数；超额因素经确认后只执行当前方法对应阶数的 `C(n,k)` 组合。
- 保留旧版统一 `manova` 计划迁移：按保存阶数或原因素数自动转换到新方法，并移除旧阶数参数。
- 修复 MANOVA 交互效应标签还原的子串替换错误，包含 `F1`/`F2` 字样的原始因素名不再被重复改写。
- 新增固定 MANOVA 入口、必要设置、超额组合、旧计划迁移和特殊列名专项测试；单元与集成测试合计 164 项通过。

## 0.4.6

- 完成从数据读取、角色选择、阶数/组合确认、预检、参数序列化、统计执行、事后比较、CLD、AI 辅助、工作区保存恢复到 Markdown/Excel 报告的发布前全链路审查。
- t 检验 η²改为精确的 `t²/(t²+df)`；配对 Hedges 校正使用配对差值自由度；效应量结果补充校正自由度与区间方法元数据。
- 修复零误差方差但组均值不同的 t/EMM/事后比较被误判为不显著的问题；退化情形现在返回有方向的无穷统计量和正确显著性。
- Mann–Whitney 秩二列相关方向统一为“第一组较大为正”，并增加参数有效性检查。
- Kruskal–Wallis 显著后的跟进由逐对 Mann–Whitney 替换为真正的 Dunn 检验：全局秩、并列值修正和 Holm 校正。
- 二元 Logistic 回归改为报告真正的 McFadden `R² = 1 - LL_model/LL_null`，离差解释率作为独立指标保留，不再混称。
- Tukey–Kramer 与 statsmodels 不平衡样本结果、双因素 Type III ANOVA 表、MANOVA 四种多元统计量和 Logistic 指标完成独立参考交叉验证。
- CLD 增加穷举性质测试：五个水平全部 1024 种显著关系图均满足“显著组不共享字母、非显著组至少共享字母”。
- MANOVA 组合分析的跨任务 p 值按多元统计量名称分别形成假设族，不再把 Pillai、Wilks、Hotelling–Lawley 和 Roy 混入同一校正族。
- 重复测量和混合设计的配对对比修正 p 值与置信区间标签保持一致；REML 下无定义或非有限 AIC/BIC 不再进入结果表。
- 常数数据的正态性/方差齐性检查改为显式退化诊断，避免底层运行时警告干扰用户。
- 发布工具升级：PyInstaller 产物统一为 `datawork-sidecar`；新增版本、执行器、静态资源、文档和 Python 编译发布就绪检查；增加跨平台构建与发布清单。
- 实际 PyInstaller 构建修复 spec 根目录多取一层的问题；在清洁依赖环境中生成 Linux sidecar，并限制 Matplotlib 为 Agg、排除测试/Notebook/深度学习等非运行时模块。
- 修复 pandas 3 / NumPy 新版中相关矩阵 `to_numpy()` 可能只读导致 MANOVA 预检崩溃的问题，填充对角线前显式复制数组。
- 155 项测试按独立分区全部通过；236 组注册高级参数和 360 组数据结构变体完成实际审查，其中 180 个合法配置成功执行；前端 TypeScript、生产构建和 Playwright 工作流审查通过。
- 最终交互优化新增工作流进度条、推荐角色恢复、缺失选择即时提示、参数默认恢复、组合任务清单、结果失效原因和 Esc 关闭弹窗；界面版本号改为读取 `/api/health`，避免发布后显示旧版本。
- AI 操作助手由遮罩抽屉升级为可与主界面并行使用的悬浮窗：支持拖动、右下角缩放、最小化、恢复默认布局和关闭；桌面端自动保存位置/尺寸，窗口越界后自动收回，移动端退化为安全的近全屏布局。

## 0.4.5

- 新增显著性紧凑字母分组（CLD）：根据完整成对比较矩阵生成 `a / ab / b`，并以防御性规则保证显著组不共享字母、非显著组至少共享一个字母。
- Tukey–Kramer、Games–Howell、Holm、Bonferroni、Šidák、FDR-BH、Scheffé、Duncan 与 Fisher protected LSD 可生成各自独立的字母分组；Dunnett 因比较矩阵不完整而明确不生成完整 CLD。
- 网页、项目工作区、Markdown 报告、Excel 报告和组合分析汇总均新增显著性字母分组输出。
- 新增统一的“单/双/三因素及多候选因素组合方差分析（阶数控制）”入口：候选因素数量与单个模型阶数分离，阶数 1/2/3 分别建立单、双、三因素完整析因模型。
- MANOVA 新增同一套因素阶数控制；例如 3 个候选因素配合阶数 1 或 2，分别执行 `C(3,1)` 或 `C(3,2)` 个联合因变量模型。
- 超出阶数时组合实验仍默认关闭；用户必须在弹窗中确认模型数量与跨任务校正后才会执行。
- 事后检验参数由单选升级为可单选/多选，保留方法默认值，并在界面内说明 Tukey、Duncan、Games–Howell、Dunnett 的适用差异。
- 旧版 `posthoc_method` 计划自动迁移为 `posthoc_methods`，旧 MANOVA 计划在未记录阶数时按原固定因素数量恢复，保持项目可复现性。
- 新增 6 项阶数、组合、多方法事后检验与 CLD 专项测试；后端共 140 项测试按分区全部通过，前端 TypeScript 与生产构建通过。

## 0.4.4

- 修复 MANOVA 联合因变量工作流：切换方法或读取数据后自动补足至少两个候选数值因变量，并在界面明确显示“联合因变量”。
- MANOVA 的用户指定 EMM、EMM 因素和对比校正现已真正传入统计引擎；不再出现界面可选但后端忽略的情况。
- MANOVA 正式支持分类因素组合批量分析；普通模式仍限制 1–3 个因素，只有用户明确启用后才展开独立组合模型。
- 新增超额因素确认弹窗：单因素方法按 `C(n,1)`、双因素方法按 `C(n,2)`、三因素方法按 `C(n,3)` 展开，并在执行前显示任务数量和跨任务校正。
- 显著交互后，双/三因素 ANOVA 与 MANOVA 自动停止相关边际主效应比较，改为经 Holm、Bonferroni、Šidák、FDR-BH 等校正的条件简单效应和条件内 EMM 比较。
- MANOVA 的 Bartlett 探索性诊断改为完整模型残差相关矩阵；因变量条件数改为标准化后计算，避免量纲差异造成误报；Box's M 使用独立阈值，默认 0.001。
- Dunnett 对照支持按因素分别填写，例如 `处理=CK;品种=对照品种`；模型 EMM 场景继续明确标注为 Bonferroni 保守近似。
- Type I 不平衡设计和 Type II 显著交互新增明确风险提示。
- Markdown、Excel 和网页结果表同步显示简单效应的原始 p、校正后 p 和校正方法。
- 新增 8 项专项回归测试；全部 134 项后端测试按模块完成通过，前端生产构建与交互契约测试通过。

## 0.4.3

- 新增分析前结构化智能统计设计审查，输出研究设计摘要、预计引擎、完整单元、重复数、平衡比、残差自由度、风险和建议动作。
- AI 分析计划新增设计审查摘要、设计检查和建议动作，严格围绕当前数据列、变量角色和统计方法说明“做什么、研究什么”。
- MANOVA 从 1–2 个对象间因素扩展到 1–3 个因素，支持完整析因主效应、两两交互和三因素交互。
- MANOVA 新增 Box's M、Mardia 残差近似诊断、Bartlett 相关矩阵球形检验、相关矩阵和条件数冗余诊断。
- MANOVA 新增按主要多元判据触发的单变量 Type I/II/III 跟进，以及跨因变量/效应的 Holm、Bonferroni、Šidák、FDR-BH 校正。
- 新增统一事后比较核心：Tukey–Kramer、Games–Howell、Holm、Bonferroni、Šidák、FDR-BH、Scheffé、Duncan、Fisher protected LSD 和 Dunnett。
- 双因素、三因素 ANOVA 与 MANOVA 的析因事后比较改为模型估计边际均值；显著交互时阻止误导性主效应自动比较并提示简单效应分析。
- 混合设计升级为自适应经典/MixedLM 双路径，支持一至两个对象间因素、一个重复因素、不平衡设计和部分重复缺测。
- MixedLM 路径报告固定效应联合 Wald χ²、随机截距方差、残差方差、收敛状态、AIC/BIC、EMM 和校正后比较。
- 修复数据列名为 `C` 时与 Patsy 分类函数冲突的问题，并确保 MANOVA 结果只展示用户实际选择的多元统计量。
- 后端 125 项自动化测试、前端 TypeScript 检查和生产构建通过。

## 0.4.2

- 完善单/双因素多元方差分析（MANOVA），明确支持 1–2 个对象间分类因素以及双因素主效应和交互作用。
- 新增 MANOVA 多元检验统计量选择：全部四种、Pillai 轨迹、Wilks' Lambda、Hotelling–Lawley 轨迹或 Roy 最大根。
- 修复 statsmodels 字段名大小写不匹配导致 MANOVA 总体检验表为空的问题。
- 修复 Hotelling–Lawley 统计量名称不匹配导致该检验无法输出的问题。
- 数值编码的分类因素现在通过 `C(...)` 强制按分类变量建模，避免 1/2/3 等处理水平被误当作连续协变量。
- MANOVA 结果表新增多元统计量名称、统计量值和近似 F；不再把不可用的偏 η²显示为 0。
- 默认排除无研究意义的截距多元检验，并新增“预先指定主要判据、避免事后挑选显著结果”的报告提醒。
- 修复精确零效应触发底层空特征根异常的问题；零效应现在正确返回 F=0、p=1。
- 新增双因素 MANOVA、统计量选择、数值因素分类化和边界条件自动化测试。

## 0.4.1

- 完成 43 种方法的服务层和 HTTP 全路径矩阵审查。
- 新增 350 个数据变体审查、100 个非默认高级参数审查和生产前端浏览器交互审查。
- 修复单列 CSV 分隔符误判导致唯一列消失。
- 修复单样本/两样本比例 z 检验的单侧备择假设映射。
- 修复有序回归 cloglog、loglog 和 cauchit 链接函数。
- 增强分类成功类别、有序水平、分层二元结构、配对类别集合和 Cochran Q 退化数据预检。
- 批量预检改为逐个验证真实子任务并提前汇总失败批次。
- 修复前端无上限角色被隐藏、方法切换残留角色和旧结果未失效。
- 项目工作区缺少必选角色时禁止保存并列出缺项。
- 数据读取、预检、分析、项目、计划、运行和报告增加统一等待状态、重复点击保护和可操作错误提示。
- 自动化测试增至 115 项。

## 0.4.0

- 完成发布级全面审查，所有 43 种方法均绑定可执行统计执行器。
- 混合设计 ANOVA 从占位状态升级为严格的一个对象间因素 × 一个对象内因素完整平衡实现。
- 重复测量增加 Mauchly 球形性检验、Greenhouse–Geisser/Huynh–Feldt 校正和重复水平两两比较。
- 线性混合模型增加最多两个随机斜率、REML/ML、优化器、随机协方差和奇异性警告。
- 新增模型设计矩阵驱动的 EMM、置信区间和多重校正对比。
- 新增结构化诊断图，网页使用轻量 SVG，报告包保存 PNG。
- 修复 REML 下无定义 AIC/BIC 被显示为 0、零方差配对差值精度警告、跨任务 p 值假设族混合等问题。
- 新增 Tauri 2 桌面 sidecar 外壳和 `/api/capabilities` 稳定契约。
- 自动化测试扩展并新增方法注册一致性、混合设计、随机斜率、EMM 与诊断报告检查。

## 0.3.6

- 方法注册表扩展为 43 种定义，其中 42 种可执行；分类相关方法增至 20 种。
- 分类因素支持多选候选池、最小/最大组合阶数和排列组合批量建模。
- 组合任务按因变量 × 因素组合 × 拆分水平展开，并导出一个“组合分析汇总”工作表。
- 新增跨组合 Holm、Bonferroni、FDR-BH 或不校正，保留原始与校正 p 值。
- 新增统一高级参数 Schema，网页默认折叠，CLI 支持 `--parameter key=value`。
- 卡方检验新增幂散度族、置换和 Monte Carlo p 值。
- 新增精确二项、比例 z、多组比例及两两比较、Barnard、Boschloo、Cochran Q、Bowker、Stuart–Maxwell/Bhapkar、CMH、Breslow–Day。
- 新增 Cohen/Fleiss/Randolph kappa、Cochran–Armitage 趋势检验、多项与有序 Logistic 回归。
- 二元 Logistic 新增成功水平、稳健协方差和迭代参数。
- 自动化测试增至 64 项。

## 0.3.5

- 统计方法注册表扩展为 27 种定义，其中 26 种可以执行。
- 新增描述统计、单样本 t、Mann–Whitney U、Wilcoxon、Kruskal–Wallis、Pearson/Spearman/Kendall、线性回归、二元 Logistic 回归、卡方独立性、Fisher 精确、卡方拟合优度、McNemar、ANCOVA、重复测量 ANOVA、Friedman 和随机截距线性混合模型。
- 每种方法新增用途、变量作用关系、变量数量与角色要求、输出指标、假设、常见场景和限制。
- 新增通用主要检验、模型系数和拟合统计结果结构。
- 方法说明、前端角色选择、预检、AI 说明、执行器和报告统一读取方法注册表。
- Markdown 与 Excel 报告支持非参数、相关、回归、列联和混合模型结果。
- AI 分析目的会直接带入当前选择的因变量、因素、协变量、随机因素和重复因素。
- 自动化测试增至 58 项。

## 0.3.4

- AI 侧边栏改为固定标题和关闭按钮，内容独立滚动；主页面只保留分析目的与预期效果摘要。
- 新增后端驱动的分析前检查弹窗，明确缺失角色、冲突、样本不足和失败批次。
- 移除特定 T/CK、品种、种植模式和 DR/NDR/∆DR 业务流程。
- 新增完全通用的多因变量 × 任意拆分列批处理，并输出单个合并结果表。
- 自动化测试增至 50 项。

## 0.3.3

- 新增成对处理-对照一键批量工作流，自动识别 Tn/CKn、调查时间列、品种、种植模式和重复来源。
- 自动派生 DR、NDR、∆DR，并一次生成双因素方差分析、品种比较和种植模式比较三张表。
- 新增 `POST /api/batch/paired-control` 与批量结果下载接口。
- 网页新增一键批量卡片、结构识别摘要、结果预览和 XLSX 下载。
- 新增 `datawork batch-auto` CLI 命令。
- 清洗逻辑现在始终排除空标题、Unnamed 和全空列；即使占位列有零星误填内容也不会进入分析。
- 清洗日志新增 `excluded_columns`、排除原因和非空单元格数量。
- 使用项目测试数据生成的表1、表2、表3与参考工作簿逐单元格一致。
- 自动化测试增至 47 项。

## 0.3.2

- 分析计划区新增基于当前数据和当前方法的“分析目的 / 预期效果”实时说明。
- AI 输出改为 Pydantic 结构化字段，前端分区渲染，解决 Markdown、换行和列表显示混乱。
- 新增 `POST /api/ai/explain/analysis-plan`，支持确定性说明与用户主动触发的 AI 优化。
- AI 连接测试升级为连接与模型发现一体化流程，支持 OpenAI、DeepSeek、Gemini、Ollama 和 OpenAI 兼容接口。
- 模型列表统一展示 Model ID、名称、所有者、能力、输入/输出限制、本地参数规模、量化方式和可选择状态。
- 每次探测后强制回到“等待手动选择”状态，不自动沿用旧模型或选择首个模型。
- 兼容接口未实现 `/models` 时提供显式错误和手动 Model ID 入口。
- 自动化测试增至 43 项。

## 0.3.1

- 新增网页 AI 设置中心，支持服务商、Base URL、模型、密钥、隐私范围和连接测试。
- 新增跨平台密钥策略：当前会话内存或操作系统 Keyring；密钥不进入 SQLite、配置 JSON、报告和日志。
- 新增内置操作向导和首次启动引导，不配置 API 也可解释每个关键步骤。
- 新增 AI 步骤解释和结果阅读 API；结果解释经过统计数值 Guard，失败时回退为确定性说明。
- 默认隐私模式不发送原始数据行；可显式允许最多 5 行预览。
- 远程绑定时默认禁用 AI 调用，新增 `--allow-remote-ai` 管理员开关。
- 前端增加 AI 状态、操作助手抽屉、结果解读入口和设置对话框。
- 自动化测试增至 37 项。

## 0.3.0

- 将数据读取、设计建议、统计执行和报告生成统一收口到 application 层。
- 方法注册表新增明确的执行器标识，`AnalysisService` 不再维护方法分支链。
- 新增结构化错误代码、校验问题严重级别和兼容旧客户端的错误响应。
- 每次分析记录计划 SHA-256、源数据和清洗数据指纹、清洗日志、运行参数、Python/系统/依赖版本。
- 新增跨平台 SQLite 项目工作区：项目、数据集、计划修订、运行历史、报告与结果对比。
- 数据集运行前重新校验源文件哈希，工作区文件采用原子写入。
- 新增项目工作区 REST API 和 ZIP 报告下载。
- CLI 与兼容 Streamlit 界面开始统一使用应用服务。
- 新增 Windows、Ubuntu、macOS × Python 3.11–3.13 CI 矩阵。
- 自动化测试增至 27 项。

## 0.2.0

- 新增 FastAPI + Vue 跨平台网页界面。
- 新增 AnalysisPlan、Method Registry 和 AnalysisService。
- 删除硬编码 API Key 与裸 `eval()`。
- 修复 Welch ANOVA、配对 t 检验和 Type III 平方和。
- 阻止未实现方法静默降级。
- 增加跨平台数据读取、启动、测试和发布基础。
