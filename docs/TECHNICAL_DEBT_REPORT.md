# DataWork 0.4.9 轻量技术债清理报告

扫描与处理日期：2026-07-20

## 扫描范围

覆盖 `datawork/`、`frontend/src/`、`desktop/src-tauri/src/`、`scripts/` 与 `tests/`。依赖目录、虚拟环境、构建目录、缓存和生成产物不参与源码注释统计。

## TODO 与过程注释

对 `TODO`、`FIXME`、`HACK`、`XXX` 的全量源码扫描结果为 **0 项**。测试代码完整保留，未通过删除测试来降低遗留项数量。

## 已安全移除

- Python：移除 CLI、设计推断、公式、报告、假设检验、决策树、批处理、分类方法、效应量、约束和旧 UI 中可静态证明未使用的导入。
- 前端：移除 `App` 中未使用的 `DiagnosticPlotPanel` 导入。
- AI 服务：移除未使用的 `ModelInfo` 导入。
- 文档：删除与 `INTERACTION_AUDIT.md` SHA-256 完全一致的重复副本；将过期实施摘要中的有效记录并入 `DEVELOPMENT_LOG.md` 后删除原文件。
- 发布配置：补齐 `.audit`、`.tmp`、带前缀的 pytest 临时目录、`.ui-*` 和 Vite/TypeScript 缓存排除规则。

以上变更仅涉及无引用导入、完全重复文档与发布边界，不改变统计方法、API 契约或核心业务架构。

## 依赖结论

逐项核对 Python 直接依赖、前端运行依赖和桌面构建依赖后，未发现可以在不改变现有功能或构建链的前提下安全删除的外部依赖。因此本次未修改依赖声明。

`.venv`、`frontend/node_modules`、`desktop/node_modules` 与 Rust `target` 是本地开发依赖/构建缓存，不进入源码发布包；保留工作副本仅用于本地复验，不视为发布资产。

## 保留项与边界

- 保留全部 `tests/`、黄金数据与审计脚本。
- 保留 `USER_MANUAL.md`、`DEVELOPMENT_LOG.md`、`CHANGELOG.md` 以及历史审计证据。
- `__init__.py` 中的公共导出和统计公式环境中的动态符号可能被外部调用或运行时解析，未按普通“未引用导入”处理。
- 未做大规模架构重构、API 改名或统计实现替换。

## 验证结果

- pytest：260 项通过，0 项失败。
- 结构发布检查：47 个可运行方法与 47 个执行器一致。
- 黄金数据：52 个案例、425 项检查、0 项失败。
- 参数矩阵：344 个变体通过。
- 数据变体：387 个配置完成审计，其中 190 个合法配置执行成功。
- 前端生产构建：通过。

