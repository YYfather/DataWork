个人主页副本变更说明
====================

本包从 E:\studywork\Web 只读复制生产文件，并只对副本执行以下两项修改：
1. index.html：新增 DataWork 项目卡片，并把 DataWork 加入控制台连通性检查；
2. sw.js：把 /datework/ 加入子应用放行列表，并更新主页缓存版本。

原目录、原文件、Git 状态和现有未提交修改均未被写入或清理。
服务器部署时，增量覆盖文件用于 /www/wwwroot/1490473838.cn；原站点保持 PHP 项目，
DataWork 运行文件放在 /www/wwwroot/1490473838.cn/datework，并由独立 Python 服务和
/datework/ 反向代理提供。
“主页增量覆盖”只包含这两个修改后的文件；“主页完整副本”是排除开发目录、缓存、
数据库、日志、密钥和备份文件后的生产文件副本。
